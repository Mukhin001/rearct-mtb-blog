import st from './content.module.css';

const Content = (props) => {
    return (
        <div className={st.Content}>
            {/* {props?.content} */}
           
        </div>
    );
};


export default Content;
